#installation steps
sudo apt-get install i3
sudo apt-get install suckless-tools i3status
sudo apt-get install i3lock

#key bindings
mod + enter             -   open a terminal
mod + shift + q         -   quit an application
mod + v                 -   tells i3 to insert in vertically
mod + arrow keys        -   to move to windows    
mod + s                 -   to go in stack mode
mod + e                 -   to go in tiling mode
mod + w                 -   to go in tab mode
mod + r                 -   to go in resize mode(use arrow keys to increase the size)

mod + shift + arrowkeys - to change the window location

mod + shift + number    - to move that current application to that workspace

mod + shift + e         -  to logout

#in terminal enter i3lock    -   this will lock your screen
#to lock screen use custom shortcut
#go to cofig file of i3 using the i3-config-wizard and add this line to end of the file

                bindsym $mod+shift+x exec i3lock

#save the file 
#and restart i3 by pressing
                mod+shift+r


in terminal enter i3-config-wizard - this will show the i3 config directory

# run below code to enable tap to touch in trackpad
sudo mkdir -p /etc/X11/xorg.conf.d && sudo tee <<'EOF' /etc/X11/xorg.conf.d/90-touchpad.conf 1> /dev/null
Section "InputClass"
        Identifier "touchpad"
        MatchIsTouchpad "on"
        Driver "libinput"
        Option "Tapping" "on"
EndSection

EOF

#install playerctl
#go to google and search for playerctl
#in github section go to releases and download the debian package
#then in the folder in which it is downloaded
sudo dpkg -i playerctl_debian_package_name

# add the below line commands in the i3 config file
# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Touchpad controls
#bindsym XF86TouchpadToggle exec toggletouchpad.sh # toggle touchpad

# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

#To load applications on runtime just type below in i3 config file and reload i3
exec application_name
#To always load application on runtime
exec_always application_name

#To change background and reload everytime when we reboot 
#we need to install feh and
#add the below command to i3 config file
sudo apt-get install feh
exec_always feh --bg-scale absolute/path/to/wallpaper.jpg

#To configure the monitors download below
sudo apt-get install arandr
#configure your another screen and save the file 
#the file will be saved in .screenplay folder as a shell script file
#in i3 config file add the shell script result as 
exec_always full_arandr_command

#in config file you can rename workspace number to a name
#replace the number with a variable in two places for example
set $workspace1 "1:browser"
#assign this variable to the workspace number